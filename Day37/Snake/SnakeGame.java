/**
 * SnakeGame class contains the main method to start the Snake game application.
 */
class SnakeGame {
    /**
     * Main method creates a new instance of GameFrame, which initializes the game.
     * 
     * @param args Command-line arguments (not used in this application)
     */
    public static void main(String[] args) {
        new GameFrame(); // Creates a new instance of GameFrame to start the game
    }
}
